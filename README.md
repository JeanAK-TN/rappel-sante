
  # Global app interface mockup

  This is a code bundle for Global app interface mockup. The original project is available at https://www.figma.com/design/yfClKWu7FmsBhDCj2pozsu/Global-app-interface-mockup.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  